# Binance ML Scalping MVP — переменные окружения
# Скопируй этот файл: copy .env.example .env
# Затем заполни свои ключи.

# ─── Binance API (нужны только для REST depth snapshot) ──────────
# Можно оставить пустыми — depth snapshot работает и без ключей,
# но с ключами лимиты выше.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# ─── Web панель ──────────────────────────────────────────────────
WEB_HOST=127.0.0.1
WEB_PORT=8080

# ─── Logging ─────────────────────────────────────────────────────
# DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
